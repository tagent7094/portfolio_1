You are rewriting the opening line of a LinkedIn post.

YOUR JOB: Generate {n} opening lines that a REAL HUMAN would write — not an AI,
not a copywriter template, not a LinkedIn guru with a content calendar.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CRITICAL ANTI-AI RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Your openings MUST NOT:
  x Start with "Here's the thing" / "Let me tell you" / "Hot take" / "Unpopular opinion"
  x Use "In today's [anything]" or "In the world of [anything]"
  x Use "What if I told you" / "Imagine this" / "Picture this"
  x Use "Let that sink in" / "Read that again" / "I'll wait"
  x Use "Nobody talks about" / "Most people don't realize" / "The truth is"
  x Use "Game changer" / "This changed everything" / "Buckle up"
  x Use emoji as the first character
  x Use more than 1 exclamation mark total
  x Sound like a TED talk intro or a self-help book chapter title
  x Be a question that is really a disguised statement ("Isn't it crazy that...")
  x Reuse any phrasing from the current opening below
  x Be so polished it sounds generated — rough edges are good

Your openings MUST:
  -> Sound like something typed by a specific person with a specific experience
  -> Contain at least one concrete detail: a number, a name, a place, a time, a quote
  -> Be tightly coupled to THIS post's content — could not be swapped into any other post
  -> Feel slightly imperfect, like real speech, not polished copywriting
  -> Create a gap the reader needs to close — curiosity, tension, or surprise

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CURRENT OPENING (the line you are replacing — do NOT reuse its phrasing)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{current_opening}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
POST BODY (context — your opening must connect naturally to this)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{post_body}

{viral_block}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AUTHOR VOICE (match this human's actual tone, not a cleaned-up version)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{personality}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
HOOK STRATEGIES (generate one opening per strategy, in this order)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{strategy_block}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
QUALITY BAR — before finalizing each line, ask:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  -> Would this specific author actually say this in a voice memo?
  -> Does it contain at least one detail too specific to be invented?
  -> Would a reader stop scrolling, or keep going?
  -> Is it under 15 words? (shorter is almost always stronger)
  -> Could this opening be dropped into a different post and still work?
     If yes — it is too generic. Rewrite it.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Return a JSON array of exactly {n} objects:
[
  {{"id": "opening_1", "text": "your opening line here", "strategy": "mid_story_drop"}},
  ...
]

Rules for the output:
  - "text" is ONLY the opening line(s) — not the full post
  - Each opening is 1 to 3 sentences max. One sentence is usually better.
  - Shorter is almost always stronger. Aim for under 15 words on the first sentence.
  - Return ONLY the JSON array. No explanation, no markdown fences, no preamble.
