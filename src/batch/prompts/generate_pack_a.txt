You are generating a BATCH A (mirrored opening) post. Keep the source hook's sentence shape, beat order, and compression. Replace every concrete noun, domain, and specific with the founder's world.

Same bones. New content. Founder's voice.

## SOURCE POST
{source_post}

## SOURCE DISSECTION
{dissection}

## SOURCE HOOK MECHANIC — YOUR OPENER MUST USE THIS (NON-NEGOTIABLE)
Primary mechanic: {source_hook_mechanic}
How it works: {source_mechanic_description}

Source opener (exact text):
{source_opener_text}

HARD RULE: Your opener MUST use the source's primary mechanic ({source_hook_mechanic}).
- If the source opens with "specific_number" → your opener needs a specific number from the founder's world
- If the source opens with "authority_quote" → your opener needs a quote from the founder's cast
- If the source opens with "scene_entry" → your opener needs a scene from the founder's signature scenes
- Do NOT default to the founder's signature mechanic. The founder's VOICE applies. The source's MECHANIC applies.
- If the source addresses the audience directly ("Founders,"), your opener must address the audience directly
- If the source anchors authority with a credential ("I've backed 40+ companies"), your opener must anchor with the founder's credential
- If the source makes a numbered promise ("here are 5 patterns"), your opener must include a numbered promise

## SOURCE BODY FORMAT — YOUR BODY MUST USE THIS (NON-NEGOTIABLE)
Body format: {source_body_format}
Item count (if applicable): {source_body_item_count}

HARD RULE: Your body MUST use the source's body format. Do NOT collapse a numbered list into flowing prose. Do NOT expand a single scene into multiple parallel paragraphs.

- If `numbered_list` → your body MUST be a numbered list with discrete items. Match the item count within ±1 (so if source has 7 items, generate 6-8 items; if source has 5, generate 4-6). Each numbered item gets its own paragraph or short block. The list IS the body. Do NOT write it as flowing prose with "first... then... finally..." — actual numbers (1., 2., 3., …) must appear.
- If `bullet_list` → use bullets (•, -, *) for each discrete unit.
- If `parallel_paragraphs` → write 2-4 paragraphs that open with the same shape and build the same way.
- If `three_examples` → present three distinct examples, each clearly delimited (could be three short paragraphs, three numbered items, or three named scenes).
- If `before_after` → structure as before-pivot-after.
- If `progressive_revelation` → flowing build is allowed.
- If `single_scene` → one continuous scene, no list structure.
- If `prose_essay` → flowing argumentative prose, no list structure.

The single most common failure mode for Batch A is: source is `numbered_list` but the generated post writes a flowing contrarian essay that mentions the same themes. This is a HARD STRUCTURAL FAILURE. The mold of the source must transfer.

## SOURCE CLOSER MECHANIC — YOUR CLOSER MUST USE THIS (NON-NEGOTIABLE)
Closer mechanic: {source_closer_mechanic}
Source closer text: {source_closer_text}

HARD RULE: Your closer MUST use the source's closer mechanic.
- If source closer is `cta` (e.g., "I put together the breakdown of these 7 companies — link in comments"), the generated post MUST close with a parallel CTA referring to the founder's own work / breakdown / playbook / DM offer.
- If source closer is `reframe_question`, the generated post MUST close with a question that flips the reader's posture.
- If source closer is `terminal_verdict`, the generated post MUST close with a declarative final line.
- If source closer is `physical_image`, the generated post MUST close with a concrete physical image / detail.
- If source closer is `challenge`, the generated post MUST close with a direct challenge ("Stop X. Start Y.").
- If source closer is `callback`, the generated post MUST call back to a phrase or image from the opening.
- Do NOT swap CTA → reframe question. Do NOT swap terminal verdict → physical image. The closer carries the source's energy into the reader.

## OPENER QUALITY TESTS (from source dissection)
{opener_tests}
If any test FAILED, do NOT replicate that quality in your opener.

## FOUNDER INTERNALIZATION
{internalization}

## FOUNDER VOICE MARKERS
{voice_markers}

## FOUNDER PERSONALITY CARD
{personality_card}

## WORD COUNT RANGE
{word_count_range}

## FORMATTING HABITS
{formatting_habits}

## EVENTS ALREADY USED (do NOT reuse any of these)
{events_used}

## STORIES ALREADY USED (do NOT reuse)
{stories_used}

## VARIANT NUMBER: {variant_number} of 3

## PRIOR A VARIANTS FROM THIS SOURCE (avoid repeating their angles)
{prior_a_posts}

---

## BATCH A RULES

1. MIRROR the source's opening MECHANIC and sentence shape EXACTLY:
   - Same PRIMARY MECHANIC — if the source uses specific_number, you use specific_number with the founder's numbers. If the source uses scene_entry, you use scene_entry with the founder's scene. Do NOT substitute a different mechanic.
   - Same number of sentences in the hook
   - Same structural functions (if source opens with scene drop → you open with scene drop)
   - Same rhythm (short-short-long → short-short-long)
   - Same beat order (setup → complication → reveal)
   - If the source addresses the audience directly, you must address the audience directly
   - If the source anchors with a credential/number, you must anchor with the founder's credential/number
   - The SOURCE author's typography, lowercase, emojis, register — those are THEIR voice, not the hook's engine. Replace with the founder's voice.

2. REPLACE ALL CONTENT:
   - Every domain reference → founder's domain
   - Every specific number → founder's specific number (from key_moments_inventory)
   - Every name → founder's cast member
   - If a named entity from the source matches the founder's recurring_cast, KEEP that name — do not replace it
   - Source cast names: {source_cast}
   - Every scene → founder's signature scene
   - If the source's register does mechanical work (rant energy, vulnerability, exasperation) — translate that energy into the founder's register, don't strip it.

   FIRST-DEGREE FABRICATION CHECK:
   If the source opener uses a first-person narrative scene ("I received an email from X,"
   "I sat across from Y," "I recently met a..."), do NOT fabricate an analogous scene for the founder.
   Fabrication is forbidden even when mirroring. Three options:
   a) Use a VERIFIED scene from the founder's signature_scenes list (the alias assignment, Series B rejection, 106 flights, the Sanas Daytona moment, etc.)
   b) Translate to a third-degree pattern ("Every founder I back at Carya...")
   c) Translate to a verifiable second-degree scene ("A portfolio company CEO recently...")
   Never invent a specific scene, quote, or interaction the founder hasn't documented.
   If the founder's internalization has signature_scenes — use those. Don't create new ones.

3. ANTI-RECYCLING (critical):
   - If a sentence could be copy-pasted from one of the founder's published posts, rewrite it until it can't.
   - A punchline they've used must be earned through different setup, or dropped entirely.
   - Treat their existing posts as RAW material, not finished material.

4. EVERY POST MUST:
   - Hit the word count range (±30% of median)
   - Match the founder's formatting habits exactly
   - Orbit one scene the reader can physically picture
   - Put the founder INSIDE the experience — vulnerable, exposed, not watching from outside
   - Deliver exactly what the hook promised — the hook sets one specific argument in motion and every paragraph must advance THAT argument, not drift to adjacent material
   - End on a concrete detail — a name, an image, a physical object, a challenge, a quiet admission. NOT a summary, NOT a moral, NOT a restatement of the thesis
   - Not read like a rearrangement of an existing founder post

5. ENDING CHECK:
   If the ending feels over-explained, cut the second-to-last paragraph. The signal usually ends one paragraph before the AI stops writing.

6. VOICE FIDELITY:
   - Use the founder's voice markers (sentence shapes, closing moves, word choices)
   - No AI slop ("In today's...", "Here's the thing", "Game-changer", "Let that sink in", etc.)
   - If the source's opener reads as a press release, corporate announcement, or marketing copy, DO NOT mirror its register. Mirror its STRUCTURE (sentence count, beat order, rhythm) but replace its register with the founder's posture. The opener shape carries over. The opener energy comes from the founder.

6.5. VOICE COMPLIANCE (HARD RULES — non-negotiable):
   The founder card's voice OVERRIDES the source's register in every conflict.
   - If the source register is "professional concern" and the founder's posture is "controlled frustration channeled into structural analysis," use the founder's posture
   - The source's register is informational context, not a constraint on the output
   - The generated post must contain AT LEAST 3 of the voice markers listed above — integrated naturally, not sprinkled
   - The opening must match the argument rhythm pattern documented in FOUNDER INTERNALIZATION
   - The closer must be a concrete detail matching the founder's closer patterns — NOT a summary, NOT a restatement
   - Before returning, verify: would this founder read this post and think "this is what I was going to say"? If they'd rewrite more than one sentence, regenerate
   - FAIL CHECK: If the post reads like a corporate announcement, a press release, or a marketing statement — even if the source was one — the voice work failed. Regenerate from the founder card, not the source register

7. MODE:
   - Specify whether this post is "wrestling" (exploring tension, uncertain, the turn is the point) or "declaring" (stating position, confident, defending in a straight line)
   - Match the source post's mode if mirroring

8. STRUCTURAL VERIFICATION (before returning):
   Count your opener sentences. Compare against the source dissection's sentence_count and hook_mechanics.
   - Your opener must have the SAME number of sentences as the source's opener
   - Each sentence must serve the SAME structural function (from hook_mechanics[].structural_function)
   - The rhythm pattern (short/medium/long sequence) must match
   - Your BODY must match the source's body_format (`{source_body_format}`). If source is `numbered_list`, your body must contain visible numbered items (1., 2., 3., …). If source is `parallel_paragraphs`, your body must have parallel paragraph openings.
   - Your CLOSER must match the source's closer_mechanic (`{source_closer_mechanic}`).
   If any of these don't match, revise the post before returning.
   Set structural_match to true only if ALL conditions hold (opener mechanic + opener shape + body format + closer mechanic).

## OUTPUT FORMAT

```json
{{{{
  "text": "the full post text — hook + body + close as one continuous unit",
  "mode": "wrestling|declaring",
  "events_used": ["event 1 from inventory", "event 2"],
  "stories_used": ["story reference"],
  "argument_compressed": "one sentence summary of what this post argues",
  "structural_match": true
}}}}
```

No preamble. No explanation of which mechanic was used. No labeled hook/body split inside the post. No meta-commentary. Just the finished post.
